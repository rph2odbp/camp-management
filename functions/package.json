{
  "name": "camp-management-functions",
  "private": true,
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "scripts": {
    "clean": "rimraf lib",
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && firebase emulators:start --only functions,auth,database",
    "deploy": "npm run build && firebase deploy --only functions",
    "lint": "echo \"add eslint if desired\"",
    "test": "echo \"add unit/integration tests\""
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^4.4.1"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.6.0"
  }
}

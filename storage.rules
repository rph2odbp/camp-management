rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read and write access to all authenticated users.
    // This is a permissive rule for development purposes.
    // We can refine this later with more specific security requirements.
    allow read, write: if request.auth != null;
  }
}
